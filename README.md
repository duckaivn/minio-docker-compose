# MinIO Server with Docker Compose
Building an object storage server with MinIO using docker/docker-compose

## Install
```bash
git clone https://github.com/duckaivn/minio-docker-compose.git
cd minio-docker-compose
docker-compose up -d
```